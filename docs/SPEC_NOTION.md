# 모두의 모임 - 상세 기능 명세서

> 버전: 1.0
> 작성일: 2026-03-17
> 프로젝트: 모두의 모임 - 프로필 매칭 관리 사이트

---

## 1. 전체 서비스 플로우

### 1-1. 회원 가입 ~ 이용 플로우

```mermaid
flowchart TD
    Start([사이트 접속]) --> Landing[랜딩 페이지]
    Landing --> GoogleBtn[구글로 로그인 버튼]

    GoogleBtn --> GoogleAuth[Google OAuth 인증]
    GoogleAuth --> FirstCheck{최초 가입?}

    FirstCheck -->|최초| Page1["1페이지: 프로필 정보 입력<br>본명, 닉네임, 성별, 출생년도,<br>키, 거주지, 흡연, MBTI, 학력,<br>직장, 직업, 연봉, 매력, 연애스타일,<br>전화번호, 사진(최대5장)"]
    Page1 --> Page2["2페이지: 이상형 정보 입력<br>출생년도, 키, 거주지, 흡연,<br>MBTI, 학력, 직장, 직업, 연봉,<br>최애 포인트 4가지"]
    Page2 --> Submit[가입 완료]
    Submit --> PendingMsg[승인 대기 중입니다 안내]

    FirstCheck -->|기존 회원| StatusCheck{회원 상태 확인}

    StatusCheck -->|pending| PendingLogin[승인 대기 중 안내]
    StatusCheck -->|blocked| BlockedLogin[서비스 이용 제한 안내]
    StatusCheck -->|rejected| RejectedLogin[가입 반려 안내]
    StatusCheck -->|active + 여성| FemalePage["여성 페이지<br>/female"]
    StatusCheck -->|active + 남성| MalePage["남성 페이지<br>/male"]
    StatusCheck -->|관리자| AdminPage["관리자 페이지<br>/admin"]
```

### 1-2. 여성 회원 플로우

```mermaid
flowchart TD
    FemaleHome["/female<br>남성 프로필 갤러리"] --> CooldownFilter["7일 쿨타임 남성 제외<br>거절된 남성 목록에서 숨김"]
    CooldownFilter --> Filter[이상형 필터 적용]
    Filter --> BrowseCards[남성 프로필 카드 열람]

    BrowseCards --> ClickCard[카드 클릭]
    ClickCard --> MaleDetail["남성 상세 페이지<br>/female/maleId"]

    BrowseCards --> HeartIcon[하트 아이콘 클릭]
    MaleDetail --> HeartBtn[매칭 요청 목록 담기 버튼]

    HeartIcon --> Cart["매칭 요청 목록<br>/female/cart"]
    HeartBtn --> Cart

    Cart --> RemoveItem[개별 제거]
    Cart --> SendRequest[매칭 요청 보내기 클릭]
    SendRequest --> RequestSent[매칭 요청 전송 완료<br>목록 비워짐]

    RequestSent --> SentList["하단 고정 영역:<br>매칭 요청 보낸 남성 목록 표시"]

    SentList --> Note[여성은 요청 결과를<br>사이트에서 볼 수 없음<br>관리자가 별도 안내]
```

### 1-3. 남성 회원 플로우

```mermaid
flowchart TD
    MaleHome["/male<br>매칭 요청 갤러리"] --> HasRequests{매칭 요청<br>존재?}

    HasRequests -->|없음| EmptyState[유쾌한 안내 화면<br>+ 대기 팁]
    HasRequests -->|있음| BrowseCards["여성 프로필 카드 열람<br>MD추천 딱지 표시"]

    BrowseCards --> QuickAction["빠른 액션 버튼<br>매칭 확정 / 거절"]
    BrowseCards --> ClickCard[카드 클릭]
    ClickCard --> FemaleDetail["여성 상세 페이지<br>/male/femaleId"]

    FemaleDetail --> ActionBtn["매칭 확정 / 거절 버튼<br>대기중일 때만"]

    QuickAction --> StatusUpdate[상태 변경]
    ActionBtn --> StatusUpdate

    StatusUpdate -->|확정| Confirmed[매칭 확정 상태 표시<br>이후 관리자가 오프라인 처리]
    StatusUpdate -->|거절| Rejected[거절 상태 표시<br>7일 쿨타임 시작]
```

### 1-4. 관리자 플로우

```mermaid
flowchart TD
    AdminLogin["관리자 Google 로그인<br>지정된 이메일 1개"] --> AdminHome["/admin<br>회원 목록"]

    AdminHome --> SearchFilter["검색 + 필터<br>이름/상태/성별/매칭유무"]
    SearchFilter --> UserList["회원 목록 열람<br>페이지네이션 20건"]

    UserList --> ApproveReject["승인 / 반려 버튼<br>pending 회원"]
    UserList --> BlockUnblock["차단 / 해제 버튼<br>active/blocked 회원"]
    UserList --> ClickUser[행 클릭]

    ClickUser --> UserDetail["회원 상세<br>/admin/userId"]

    UserDetail --> InlineEdit["프로필 인라인 수정<br>드롭다운 + 더블클릭"]
    UserDetail --> SetExpiry[만료일 설정<br>캘린더 날짜 선택]
    UserDetail --> ViewMatches[매칭 내역 열람<br>관련 매칭 요청 + 상태]
    UserDetail --> AdminMemo["관리자 메모<br>작성/수정/삭제"]
    UserDetail --> MDRecommend["MD 추천<br>남성에게 여성 프로필 추천"]
    UserDetail --> CreateLink["프로필 공유 링크 생성<br>5일 만료, 링크 복사"]
```

### 1-5. 프로필 공유 링크 플로우

```mermaid
flowchart TD
    Admin[관리자] --> SelectUser[아무 회원 선택]
    SelectUser --> GenLink[프로필 링크 생성<br>토큰 기반, 5일 만료]
    GenLink --> CopyLink[링크 복사]
    CopyLink --> SendExternal["외부 전달<br>카톡/문자 등"]
    SendExternal --> Recipient["링크 수신자"]
    Recipient --> OpenLink["/profile/token 접속<br>로그인 불필요"]
    OpenLink --> ValidCheck{만료 여부<br>확인}
    ValidCheck -->|유효| ViewProfile["프로필 열람<br>연락처/이메일/비밀번호 제외"]
    ValidCheck -->|만료| ExpiredMsg[링크가 만료되었습니다 안내]
```

---

## 2. 화면별 상세 기능 명세

### 2-1. 회원가입 페이지 (/register)

| 구분 | 설명 |
|------|------|
| 경로 | /register |
| 접근 권한 | Google 로그인 완료 후 최초 접속 시 자동 이동 |
| 목적 | 신규 회원 프로필 등록 |

**인증:**
- Google OAuth를 통해 로그인하면 이메일은 자동 수집
- 최초 로그인 시 프로필이 없으면 이 페이지로 자동 이동

**입력 구성:**
- 1페이지: 프로필 정보 (16개 항목 + 사진)
- 2페이지: 이상형 정보 (9개 항목 + 최애 포인트 4가지)
- 상세 항목 및 선택지는 → 4장 참조

**동작:**
- 1페이지 완료 후 2페이지로 이동
- 2페이지 완료 시 가입 완료, 회원 상태는 'pending'으로 설정
- 완료 후 '승인 대기 중입니다' 안내 화면 표시

---

### 2-2. 로그인 페이지 (/login)

| 구분 | 설명 |
|------|------|
| 경로 | /login |
| 접근 권한 | 비회원 |
| 목적 | Google OAuth 인증 및 역할별 페이지 분기 |

**인증 방식:**
- 'Google로 로그인' 버튼 클릭 → Google OAuth 인증

**로그인 후 분기:**

| 상황 | 동작 |
|------|------|
| 최초 로그인 (프로필 없음) | /register 프로필 입력 페이지로 이동 |
| pending | '승인 대기 중입니다' 안내 메시지 표시 |
| active + 여성 | /female 로 이동 |
| active + 남성 | /male 로 이동 |
| blocked | '서비스 이용이 제한되었습니다' 안내 메시지 표시 |
| rejected | '가입이 반려되었습니다' 안내 메시지 표시 |
| 관리자 (admins 테이블 이메일 일치) | /admin 으로 이동 |

---

### 2-3. 여성 회원 메인 페이지 (/female)

| 구분 | 설명 |
|------|------|
| 경로 | /female |
| 접근 권한 | active 상태 여성 회원 |
| 목적 | 승인된 남성 프로필 갤러리 열람 및 매칭 요청 목록 담기 |

**레이아웃:**
- PC: 1줄 4개 카드 (그리드)
- 모바일: 1줄 2개 카드
- 이미지 중심 카드 디자인

**카드 표시 정보:**
- 프로필 사진 (대표 1장)
- 닉네임
- 출생년도
- 키
- 거주지 (시/도 + 구역)
- 직장
- MBTI

**기능:**
- 이상형 필터 (토글로 펼치기/접기)
    - 필터 항목: 출생년도, 키, 거주지, 흡연여부, MBTI, 학력, 직장 (직업은 필터 제외)
    - 필터 적용 시 실시간 목록 갱신
- 하트 아이콘: 매칭 요청 목록에 담기/빼기 (토글)
- 카드 클릭: /female/[maleId] 상세 페이지로 이동
- 상세 페이지에서 뒤로가기 시 이전 스크롤 위치 복원 (맨 위로 이동하지 않음)
- 매칭 요청 목록 페이지 이동 링크 (/female/cart)
- 7일 쿨타임 중인 남성은 목록에서 제외

**데이터 조건:**
- users.status = 'active' 인 남성만 표시
- 거절 후 7일 쿨타임 중인 남성 제외 (rejection_logs.visible_after > now())
- 최신 가입순 정렬
- 페이지네이션 (20건)

---

### 2-4. 남성 프로필 상세 페이지 (/female/[maleId])

| 구분 | 설명 |
|------|------|
| 경로 | /female/[maleId] |
| 접근 권한 | active 상태 여성 회원 |
| 목적 | 남성 프로필 전체 정보 열람 |

**표시 정보 (1페이지에 전부 노출):**
- 4장 프로필 항목 중 이성 노출 = O 인 항목 전부 (사진 포함)
- 제외: 본명, 전화번호, 이메일, 이상형 정보

**기능:**
- '매칭 요청 목록에 담기' / '매칭 요청 목록에서 빼기' 버튼 (토글)
- 뒤로가기 (갤러리로 복귀)

---

### 2-5. 매칭 요청 목록 페이지 (/female/cart)

| 구분 | 설명 |
|------|------|
| 경로 | /female/cart |
| 접근 권한 | active 상태 여성 회원 |
| 목적 | 담은 남성 프로필 확인 및 일괄 매칭 요청 전송 |

**상단 영역 - 매칭 요청 보내기:**
- 담은 남성 프로필 목록 (사진, 닉네임, 주요 정보)
- 각 항목에 개별 제거 버튼
- 개별 프로필 제거
- 프로필 클릭 시 상세 페이지 이동 (/female/[maleId])
- '매칭 요청 보내기' 버튼
    - 클릭 시 목록에 담긴 모든 남성에게 일괄 매칭 요청 전송
    - 전송 완료 후 목록 비워짐
    - 완료 안내 메시지 표시

**하단 고정 영역 - 매칭 요청 보낸 남성 목록:**
- 이미 매칭 요청을 보낸 남성들의 목록이 하단에 고정 표시
- 사진, 닉네임, 요청일 표시
- 결과(수락/거절)는 표시되지 않음 (관리자가 별도 안내)

**비즈니스 규칙:**
- 7일 쿨타임 중인 남성은 갤러리에서 이미 제외되므로 요청 목록에 담길 수 없음 (이중 안전장치)
- 이미 대기 중인 매칭 요청이 있는 남성은 중복 요청 불가

---

### 2-6. 남성 회원 메인 페이지 (/male)

| 구분 | 설명 |
|------|------|
| 경로 | /male |
| 접근 권한 | active 상태 남성 회원 |
| 목적 | 자신에게 들어온 매칭 요청 열람 및 수락/거절 |

**레이아웃:**
- PC: 1줄 4개 카드 (그리드)
- 모바일: 1줄 2개 카드
- 이미지 중심 카드 디자인

**카드 표시 정보:**
- 프로필 사진 (대표 1장)
- 닉네임
- 출생년도
- 키
- 거주지
- 직장
- MBTI
- 매칭 요청 상태 (대기중/확정/거절)
- MD 추천인 경우 'MD 추천' 딱지 표시

**기능:**
- 대기중 상태 카드: '매칭 확정' / '거절' 빠른 액션 버튼
- 카드 클릭: /male/[femaleId] 상세 페이지로 이동
- 상세 페이지에서 뒤로가기 시 이전 스크롤 위치 복원 (맨 위로 이동하지 않음)

**매칭 요청 없는 경우:**
- 유쾌한 안내 화면 표시
- 예시 문구: "많은 여성분들이 고객님의 프로필을 보고 있습니다"
- 대기 팁 안내

---

### 2-7. 여성 프로필 상세 페이지 (/male/[femaleId])

| 구분 | 설명 |
|------|------|
| 경로 | /male/[femaleId] |
| 접근 권한 | active 상태 남성 회원 (해당 여성이 매칭 요청을 보낸 경우만) |
| 목적 | 자신을 선택한 여성의 프로필 전체 정보 열람 및 수락/거절 |

**표시 정보 (1페이지에 전부 노출):**
- 2-4와 동일 (4장 프로필 항목 중 이성 노출 = O 인 항목 전부)
- 제외: 본명, 전화번호, 이메일, 이상형 정보

**기능:**
- 대기 중(pending) 요청인 경우: '매칭 확정' / '거절' 버튼 표시
- 매칭 확정 완료: '매칭이 확정되었습니다!' 메시지 표시
- 거절 완료: '거절된 요청입니다' 메시지 표시
- 뒤로가기

---

### 2-8. 관리자 메인 페이지 (/admin)

| 구분 | 설명 |
|------|------|
| 경로 | /admin |
| 접근 권한 | 관리자 (admins 테이블에 등록된 Google 계정) |
| 목적 | 전체 회원 관리 |

**관리자 인증:**
- Google OAuth 로그인 (admins 테이블 이메일 일치 시 /admin 분기, 상세는 2-2 참조)

**회원 목록:**

| 기능 | 설명 |
|------|------|
| 검색 | 이름, 전화번호로 검색 (2글자 이상 입력 시 검색 실행) |
| 필터 - 상태 | 전체/승인대기/활성/차단/반려 |
| 필터 - 성별 | 전체/남성/여성 |
| 필터 - 매칭 | 전체/매칭있음/매칭확정있음/대기중있음/거절있음/매칭없음 |
| 필터 - MD 추천 | 전체/MD추천있음/MD추천없음 (남성 회원 필터링 시) |
| 정렬 | 최신 가입순 (created_at DESC) |
| 페이지네이션 | 20건 단위 |

**회원 행 표시 정보:**
- 프로필 사진 (썸네일)
- 본명
- 닉네임
- 상태 (pending/active/blocked/rejected)
- 성별
- 출생년도, 키, 거주지, 직장 (요약)
- NEW 딱지: 가입 후 24시간 이내 회원
- 매칭 배지: 해당 회원의 매칭 건수 (전체/확정/대기/거절)
- MD 추천 배지: 남성 회원의 경우, MD 추천이 발송된 상태이면 'MD 추천' 딱지 표시 (건수 포함)
- 만료일 (설정된 경우)

**액션 버튼:**

| 회원 상태 | 가능한 액션 |
|-----------|------------|
| pending | 승인, 반려 |
| active | 차단 |
| blocked | 해제 |
| rejected | (액션 없음) |

행 클릭: /admin/[userId] 상세 페이지로 이동

---

### 2-9. 관리자 회원 상세 페이지 (/admin/[userId])

| 구분 | 설명 |
|------|------|
| 경로 | /admin/[userId] |
| 접근 권한 | 관리자 |
| 목적 | 개별 회원 프로필 열람/수정, 매칭 현황 확인, 프로필 링크 생성 |

**프로필 정보 열람 및 수정:**

| 항목 | 수정 방식 |
|------|-----------|
| 본명 | 더블클릭 → 텍스트 입력 |
| 닉네임 | 더블클릭 → 텍스트 입력 |
| 출생년도 | 드롭다운 선택 |
| 거주지 (시/도) | 드롭다운 선택 |
| 거주지 (구역) | 드롭다운 선택 (시/도에 따라 동적) |
| 학력 | 드롭다운 선택 |
| 키 | 더블클릭 → 텍스트 입력 (정수만) |
| 직장 | 드롭다운 선택 |
| 직업 | 드롭다운 선택 (직장에 따라 동적) |
| 연봉 | 드롭다운 선택 |
| 흡연 여부 | 드롭다운 선택 (유/무) |
| MBTI | 드롭다운 선택 |
| 매력 | 더블클릭 → 텍스트 입력 |
| 연애스타일 | 더블클릭 → 텍스트 입력 |
| 전화번호 | 더블클릭 → 텍스트 입력 (010-0000-0000) |

**이상형 정보 열람 및 수정:**

| 항목 | 수정 방식 |
|------|-----------|
| 이상형 출생년도 | 드롭다운 선택 |
| 이상형 키 | 드롭다운 선택 |
| 이상형 거주지 | 드롭다운 선택 |
| 이상형 흡연여부 | 드롭다운 선택 (유/무) |
| 이상형 MBTI | 드롭다운 선택 |
| 이상형 학력 | 드롭다운 선택 |
| 이상형 직장 | 드롭다운 선택 |
| 이상형 직업 | 드롭다운 선택 (직장에 따라 동적) |
| 이상형 연봉 | 드롭다운 선택 |
| 최애 포인트 4가지 | 4x2 선택형 박스 |

**만료일 관리:**
- 캘린더(date picker)로 만료일 설정
- 만료일 초과 시 자동으로 회원 상태가 blocked로 변경 (크론잡)

**매칭 내역:**
- 해당 회원과 관련된 모든 매칭 요청 목록
- 표시: 상대방 이름, 요청일, 상태 (대기중/확정/거절), 응답일

**MD 추천 (남성 상세 페이지에서만):**
- 남성 회원 상세 페이지에 'MD 추천 여성 선택' 버튼
- 클릭 시 여성 회원 목록에서 추천할 여성 선택
- 저장 시 해당 남성의 매칭 요청 갤러리에 'MD 추천' 딱지가 붙은 여성 카드 노출
- 남성은 일반 매칭 요청과 동일하게 수락/거절 가능
- 데이터: md_recommendations 테이블

**MD 추천 이력 (남성 상세 페이지):**
- 해당 남성에게 발송된 MD 추천 목록 표시
- 표시: 추천된 여성 닉네임, 추천일, 상태 (대기중/수락/거절), 응답일
- 관리자 회원 목록(2-8)에서도 MD 추천 건수가 배지로 표시됨

**프로필 공유 링크:**
- '프로필 링크 생성' 버튼
- 클릭 시 5일 만료 토큰 링크 생성
- 생성된 링크 복사 버튼
- 남녀 회원 모두 링크 생성 가능

**관리자 메모:**
- 해당 회원에 대한 메모를 자유롭게 작성/수정/삭제 가능
- 여러 건의 메모 작성 가능 (시간순 표시)
- 데이터: admin_notes 테이블
- 보안 주의사항:
    - admin_notes 테이블은 관리자 전용이며, 일반 회원에게 절대 노출되지 않아야 함
    - API 레벨에서 관리자 인증 체크 필수 (관리자 세션이 아닌 요청은 거부)
    - 회원용 API 응답에 admin_notes 데이터가 포함되지 않도록 쿼리 분리
    - Supabase RLS(Row Level Security) 정책으로 admin만 SELECT/INSERT/UPDATE/DELETE 가능하도록 설정

**상태 관리:**
- 승인/반려/차단/해제 버튼 (회원 상태에 따라 표시)

---

### 2-10. 프로필 공유 페이지 (/profile/[token])

| 구분 | 설명 |
|------|------|
| 경로 | /profile/[token] |
| 접근 권한 | 누구나 (토큰 기반, 로그인 불필요) |
| 목적 | 외부 공유용 프로필 열람 (이탈 방지 목적) |

**표시 정보:**
- 2-4와 동일 (4장 프로필 항목 중 이성 노출 = O 인 항목 전부)
- 제외: 본명, 전화번호, 이메일, 이상형 정보

**만료 처리:**
- 링크 생성 후 5일 경과 시 만료
- 만료된 링크 접근 시 '이 링크는 만료되었습니다' 안내 화면 표시

---

## 3. 비즈니스 로직 명세

### 3-1. 회원 상태 관리

| 상태 | 설명 | 전이 가능 상태 |
|------|------|----------------|
| pending | 회원가입 직후 기본 상태 | active, rejected |
| active | 관리자 승인 완료, 서비스 이용 가능 | blocked |
| blocked | 차단 상태 (수동 차단 또는 만료일 초과 자동 차단) | active (해제 시) |
| rejected | 관리자 반려 | (최종 상태) |

```mermaid
stateDiagram-v2
    [*] --> pending : 회원가입
    pending --> active : 관리자 승인
    pending --> rejected : 관리자 반려
    active --> blocked : 수동 차단 또는 만료일 초과
    blocked --> active : 관리자 해제
```

### 3-2. 매칭 요청 상태 관리

| 상태 | 설명 |
|------|------|
| pending | 여성이 요청 전송, 남성 응답 대기 중 |
| approved | 남성이 수락 (매칭 확정) |
| rejected | 남성이 거절 |

```mermaid
stateDiagram-v2
    [*] --> pending : 여성이 매칭 요청
    pending --> approved : 남성 수락
    pending --> rejected : 남성 거절
```

**매칭 확정 후:**
- 사이트에서는 'approved' 상태만 표시
- 연락처 교환 등 후속 처리는 관리자가 오프라인으로 처리

**여성 측 결과 확인:**
- 여성 회원은 사이트에서 매칭 요청의 결과(수락/거절)를 직접 볼 수 없음
- 관리자가 별도로 안내

### 3-3. 거절 후 7일 쿨타임

- 남성이 여성의 매칭 요청을 거절하면 rejection_logs에 기록
- visible_after = rejected_at + 7일 (자동 계산)
- 쿨타임 기간 동안:
    - 해당 여성은 해당 남성에게 재요청 불가
    - 해당 여성의 갤러리에서 해당 남성이 표시되지 않음
- 7일 경과 후 자동으로 재요청 가능

### 3-4. 만료일 자동 블락

- 관리자가 회원별 만료일(expires_at) 설정
- 크론잡이 주기적으로 실행하여 expires_at < now() 인 active 회원을 자동으로 blocked 처리
- 블락된 회원은 로그인 시 '서비스 이용이 제한되었습니다' 안내
- 관리자가 수동으로 해제하거나 만료일을 연장하면 active로 복귀 가능

### 3-5. MD 추천

- 관리자가 남성 회원 상세 페이지에서 특정 여성을 해당 남성에게 추천
- md_recommendations 테이블에 (admin → male_profile_id, female_profile_id) 저장
- 남성의 매칭 요청 갤러리에 'MD 추천' 딱지가 붙은 여성 카드가 노출됨
- 남성은 일반 매칭 요청과 동일하게 수락/거절 가능
- 여성 본인에게는 MD 추천 여부가 노출되지 않음
- 관리자 페이지에서 MD 추천 이력 확인 가능

### 3-6. 프로필 공유 링크

- 관리자가 아무 회원(남성/여성 모두)의 프로필 공유 링크를 생성 가능
- 토큰 기반 URL 생성 (/profile/[token])
- 링크 유효 기간: 생성일로부터 5일
- 용도: 매칭이 잘 안 되는 회원의 이탈 방지 목적으로, 관리자가 해당 회원에게 이성 프로필 링크를 외부(카톡/문자 등)로 전달
- 링크 접근 시 로그인 불필요
- 민감 정보(연락처, 이메일, 비밀번호) 제외하고 프로필 표시

---

## 4. 프로필 입력 항목 정리

### 4-1. 프로필 기본 정보 (1페이지)

> 모든 항목 필수 입력

| 번호 | 항목 | 필드명 | 입력 방식 | 선택지 | 이성 노출 |
|------|------|--------|-----------|--------|-----------|
| 1 | 프로필 사진 | photo_urls | 이미지 업로드 | 최대 5장, 필수 2장 | O |
| 2 | 본명 | real_name | 자유 텍스트 | - | X (관리자만) |
| 3 | 전화번호 | phone | 텍스트 입력 | 010-0000-0000 형식만 | X (관리자만) |
| 4 | 성별 | gender | 드롭다운 | 남, 여 | O |
| 5 | 닉네임 | nickname | 자유 텍스트 (모자이크/* 처리 없음) | - | O |
| 6 | 출생년도 | birth_year | 드롭다운 | ~2007년, 2006년 ~ 1981년, 1980년~ | O |
| 7 | 키 | height | 텍스트 입력 (정수만, 130~230) | - | O |
| 8 | 거주지 (시/도) | city | 드롭다운 | 서울, 경기, 인천, 그 외 지역 | O (합쳐서 표시) |
| 8-1 | 거주지 (구역) | district | 드롭다운 | 동부, 서부, 남부, 북부 (서울/경기만 해당) | O (합쳐서 표시) |
| 9 | 흡연 여부 | smoking | 드롭다운 | 유, 무 | O |
| 10 | MBTI | mbti | 드롭다운 | ISTJ, ISFJ, INFJ, INTJ, ISTP, ISFP, INFP, INTP, ESTP, ESFP, ENFP, ENTP, ESTJ, ESFJ, ENFJ, ENTJ | O |
| 11 | 학력 | education | 드롭다운 | 박사 졸업, 석사 졸업, SKY카포 졸업, 의대/치대/한의대/약대 졸업, 로스쿨/경찰대/사관학교 졸업, 해외대학교 졸업, 서울 4년제 졸업, 기타 4년제 졸업, 전문대학교 졸업, 고등학교 졸업 | O |
| 12 | 직장 | workplace | 드롭다운 | 대기업, 중견기업, 중소기업, 공기업, 공무원, 전문직, 개인사업/자영업, 프리랜서 | O |
| 13 | 직업 | job | 드롭다운 (직장 선택에 따른 조건부 선택지) | 추후 확정 | O |
| 14 | 연봉 | salary | 드롭다운 | ~3,000만원 미만, 3,000만원 이상~4,000만원 미만, 4,000만원 이상~6,000만원 미만, 6,000만원 이상~8,000만원 미만, 8,000만원 이상~1억원 미만, 1억원 이상~1억 5천 미만, 1억 5천 이상~2억 미만, 2억 이상~, 3억 이상~ | O |
| 15 | 매력 | charm | 자유 텍스트 (길이 제한, 디자이너 판단) | - | O |
| 16 | 연애스타일 | dating_style | 자유 텍스트 (길이 제한, 디자이너 판단) | - | O |

### 4-2. 이상형 정보 (2페이지)

> 모든 항목 필수 입력

| 번호 | 항목 | 필드명 | 입력 방식 | 선택지 |
|------|------|--------|-----------|--------|
| 1 | 이상형 출생년도 | ideal_age | 드롭다운 | ~2007년, 2006년~1997년, 1996년~1994년, 1993년~1990년, 1989년~1987년, 1986년~1984년, 1983년~1981년, 1980년~ |
| 2 | 이상형 키 | ideal_height | 드롭다운 | 130~155, 156~160, 161~165, 166~170, 171~175, 176~180, 181~185, 185~230 |
| 3 | 이상형 거주지 | ideal_city / ideal_district | 드롭다운 | 시/도 + 구역 (프로필 거주지와 동일 선택지) |
| 4 | 이상형 흡연여부 | ideal_smoking | 드롭다운 | 유, 무 |
| 5 | 이상형 MBTI | ideal_mbti | 드롭다운 | 16개 |
| 6 | 이상형 학력 | ideal_education | 드롭다운 | 프로필 학력과 동일 선택지 (10개) |
| 7 | 이상형 직장 | ideal_workplace | 드롭다운 | 프로필 직장과 동일 선택지 (8개) |
| 8 | 이상형 직업 | ideal_job | 드롭다운 (직장에 따른 조건부 선택지) | 추후 확정 |
| 9 | 이상형 연봉 | ideal_salary | 드롭다운 | 프로필 연봉과 동일 선택지 (9개) |
| 10 | 나의 최애 포인트 4가지 | top_priorities | 4x2 선택형 박스 | 출생년도, 키, 거주지, 흡연, MBTI, 학력, 직장, 연봉 중 4개 선택 (4개 선택 시 나머지 어둠 처리) |

---

## 5. 기술 스택

| 구분 | 기술 |
|------|------|
| 프레임워크 | Next.js 14 (App Router, TypeScript) |
| 스타일링 | Tailwind CSS |
| 데이터베이스 | Supabase (PostgreSQL) |
| DB 쿼리 | Raw SQL (pg / Supabase JS Client) |
| 인증 | Supabase Auth |
| 이미지 저장 | Supabase Storage |
| 배포 | Vercel |
| 크론잡 | Vercel Cron 또는 Supabase Edge Functions |
| 반응형 | 모바일 / PC 대응 |

---

## 6. 페이지 라우트 맵

| 경로 | 페이지 | 접근 권한 |
|------|--------|-----------|
| / | 랜딩 페이지 | 모두 |
| /register | 회원가입 | 비회원 |
| /login | 로그인 | 비회원 |
| /female | 여성 메인 (남성 갤러리) | active 여성 |
| /female/[maleId] | 남성 프로필 상세 | active 여성 |
| /female/cart | 매칭 요청 목록 | active 여성 |
| /male | 남성 메인 (매칭 요청 갤러리) | active 남성 |
| /male/[femaleId] | 여성 프로필 상세 | active 남성 |
| /admin | 관리자 메인 (회원 목록) | 관리자 |
| /admin/[userId] | 관리자 회원 상세 | 관리자 |
| /profile/[token] | 프로필 공유 (외부 링크) | 모두 (토큰 유효 시) |
